
apiVersion: v1
kind: Service
metadata: { name: app-next }
spec:
  selector: { app: app-next }
  ports: [{ port: {{ .Values.service.port }}, targetPort: 3000 }]
---
apiVersion: v1
kind: Service
metadata: { name: api-dotnet }
spec:
  selector: { app: api-dotnet }
  ports: [{ port: {{ .Values.service.port }}, targetPort: 8080 }]
