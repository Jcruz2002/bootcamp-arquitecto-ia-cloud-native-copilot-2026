
# Prompts para CI/CD, Docker y Kubernetes

```
Write a GitHub Actions pipeline with matrix build (.NET 10 + Next.js 16),
Docker buildx and push to GHCR, and a deploy job to AKS via Helm with environments.
```

```
Create Kubernetes manifests with readiness/liveness probes, resource requests/limits,
and an HPA. Provide a checklist to validate the deployment.
```
